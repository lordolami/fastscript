# Head-To-Head Framework Benchmarks

Public benchmark tracks:

- FastScript vs Next.js baseline
- FastScript vs Remix baseline
- FastScript vs Astro baseline

Latest run artifacts are generated via `npm run benchmark:suite` and published from the `benchmarks/` directory.
