export default function DocsV4() {
  return `
    <section class="docs-latest-page">
      <header class="sec-header">
        <p class="kicker">Current line v4</p>
        <h1 class="h1">FastScript v4</h1>
        <p class="lead">FastScript v4 publicly ratifies the platform-only story: complete TypeScript full-stack platform, ordinary TS authoring in <code class="ic">.fs</code>, visible proof apps, first-party validation, and deploy-ready outputs from one runtime boundary.</p>
      </header>

      <div class="docs-card-grid">
        <div class="docs-card">
          <p class="docs-card-title">Platform contract</p>
          <p class="docs-card-copy">FastScript v4 means pages, APIs, middleware, jobs, migrations, seeds, auth, and deploy adapters are presented as one product system, not a loose toolchain.</p>
        </div>
        <div class="docs-card">
          <p class="docs-card-title">TypeScript promise</p>
          <p class="docs-card-copy">Valid ordinary TS and JS stay first-class in <code class="ic">.fs</code>. FastScript-specific syntax remains optional sugar.</p>
        </div>
        <div class="docs-card">
          <p class="docs-card-title">Proof surfaces</p>
          <p class="docs-card-copy">Use <a href="/learn">/learn</a>, the Team Dashboard SaaS guide, Agency Ops, the support matrix, and the proof pack to inspect the platform before you commit.</p>
        </div>
      </div>
    </section>
  `;
}
