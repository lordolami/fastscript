# Next To FastScript Core Pack

1. Map `app/` routes into `app/pages`.
2. Replace segment layouts with `_layout.fs` files.
3. Move API handlers into `app/api`.
4. Verify parity with `npm run qa:all`.
